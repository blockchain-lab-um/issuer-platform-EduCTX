import type { Schema } from '../schemaTypes';

export const EuropeanDigitalCredential = {
  $id: '#europeanDigitalCredential',
  type: 'object',
  properties: {},
  required: [],
};

export const EuropeanDigitalCredentialSchema = {
  title: 'European Digital Credential',
  type: '#europeanDigitalCredential',
  fields: [],
  required: false,
} as Schema;
